# 🦐 APP-001: App and Config Pattern Examples

Status: todo
Priority: P2
Area: Apps

## Why
Shrimpy needs a living examples document for app/config ideas that emerge from real user workspaces. The goal is to make the path from simple personal automation to richer app-agent habitats concrete, without turning the examples into generic low-signal templates.

These examples should later feed setup skills, guided init flows, and agent-owned watch check-ins where Shrimpy consults with the user about deeper ways they could use the system.

## Current State
- No repo-owned app/config examples document exists yet.
- The implemented primitives examples should use are now concrete: `shrimpy agent add`, channel membership and channel-policy commands, agent-owned watches with run history, workspace/agent `vault/` and `projects/`, and skill scaffold/install commands.
- Fresh setup seeds mechanic-owned `add-agent` guidance plus shared memory-management and journal skills that can inform the practical starter examples, but it does not seed app-pattern examples.

## Build
- Create a Markdown examples document that moves from simple to complex Shrimpy app/config patterns.
- Ground examples in real workspace shapes: recurring watches, channels, agents, wake settings, skills, prompts, files, resident agents, and useful state.
- Include enough structure that the examples can become skill resources for init or advisory sessions.
- Include an opt-in "out there" examples bundle for playful/experimental configs, separate from the practical starter path.
- Connect the examples to onboarding/admin flows rather than baking them into runtime policy.

## Boundaries
- Do not implement the init skill, recurring watch/check-in loop, or app-agent behavior as part of this item.
- Do not make examples artificially generic; favor patterns that reflect serious user workspaces and durable habits.
- Do not let playful examples crowd the default setup path; they should be available as inspiration after the user has a working mechanic and main agent.
- Do not create a second app/config control plane. Examples should describe normal workspace files and Shrimpy concepts.

## Notes
- Guided onboarding can use these examples as setup resources.
- Related musing: `docs/musings/app-config-patterns.md`.
- Related musing: `docs/musings/character-agents.md`.
- Possible future uses: `shrimpy setup init` resources, admin setup skills, workspace review sessions, and recurring watch-based "how could I use Shrimpy more deeply?" check-ins.
- Related follow-up: [MECH-001](mech-001-skill-opportunity-watch.md) makes the recurring check-in concrete as an opt-in mechanic-owned watch that writes Markdown and sends the user a short recommendation message only when useful.
- Related: [channels.md](../reference/channels.md) should inform examples for async app-agent work and meaningful app channels.
- Example "out there" pattern: a Scrappy-style character agent with a specific personality, lore, and a recurring morning-letter watch. The agent is encouraged to build its own lore over time, while Shrimpy's memory and watch upkeep preserve and develop that fiction as ordinary workspace state.

## Done
- A repo-owned Markdown document exists with simple-to-complex app/config examples.
- The examples are specific enough to become skill resources later.
- The examples include both practical workspace patterns and clearly labeled optional experimental patterns.
- Recurring examples use agent-owned watches, agent-owned wake policy, and meaningful channel projections instead of introducing extra routing concepts.
- The note explains how examples support init and consultative check-ins without requiring runtime changes.
